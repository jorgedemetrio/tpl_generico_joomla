{
  "name": "tpl-generico-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes Playwright de UI do template tpl_generico (Joomla 5).",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
